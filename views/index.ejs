<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pustaka Kreatif</title>
    <style>
        body { font-family: sans-serif; background: #f0f2f5; padding: 0; margin: 0; }
        
        /* Navbar */
        nav { background: white; padding: 15px 40px; display: flex; justify-content: space-between; align-items: center; box-shadow: 0 2px 5px rgba(0,0,0,0.05); position: sticky; top: 0; z-index: 100; }
        .nav-brand { font-weight: bold; font-size: 1.2em; color: #333; }
        .nav-items { display: flex; align-items: center; gap: 15px; }
        .btn { text-decoration: none; padding: 8px 15px; border-radius: 5px; font-size: 0.9em; transition: 0.2s; font-weight: bold; }
        .btn-login { color: #333; }
        .btn-register { background: #3498db; color: white; }
        .btn-logout { background: #e74c3c; color: white; }
        .btn-add { background: #27ae60; color: white; display: flex; align-items: center; gap: 5px; }
        .profile-link { text-decoration: none; color: #555; margin-left: 10px; display: flex; align-items: center; gap: 8px; padding: 5px 10px; border-radius: 5px; }
        .level-badge { font-size: 0.8em; background: #eee; padding: 2px 8px; border-radius: 10px; color: #666; font-weight: bold; }

        .container { padding: 20px; max-width: 1200px; margin: 0 auto; }
        h1 { text-align: center; color: #333; margin-top: 20px; margin-bottom: 20px; }
        
        /* SEARCH BAR */
        .search-container { max-width: 500px; margin: 0 auto 40px auto; display: flex; gap: 10px; }
        .search-input { flex-grow: 1; padding: 12px 20px; border: 2px solid #ddd; border-radius: 50px; font-size: 16px; transition: 0.3s; outline: none; }
        .search-input:focus { border-color: #3498db; box-shadow: 0 0 8px rgba(52, 152, 219, 0.2); }
        .search-btn { background: #3498db; color: white; border: none; padding: 0 25px; border-radius: 50px; cursor: pointer; font-weight: bold; font-size: 16px; }
        .search-btn:hover { background: #2980b9; }

        /* Grid Buku */
        .book-shelf { display: grid; grid-template-columns: repeat(auto-fit, minmax(220px, 1fr)); gap: 30px; }
        .book-card { background: white; border-radius: 15px; padding: 15px; box-shadow: 0 4px 6px rgba(0,0,0,0.05); transition: transform 0.2s; display: flex; flex-direction: column; text-decoration: none; color: inherit; height: 100%; }
        .book-card:hover { transform: translateY(-5px); box-shadow: 0 10px 20px rgba(0,0,0,0.1); }
        .book-cover-img { width: 100%; height: 300px; object-fit: cover; border-radius: 10px; margin-bottom: 15px; box-shadow: 0 5px 15px rgba(0,0,0,0.1); }
        .placeholder-cover { height: 300px; background: #eee; border-radius: 10px; margin-bottom: 15px; display: flex; align-items: center; justify-content: center; color: #aaa; font-weight: bold; }
        .book-title { font-size: 1.1em; font-weight: bold; margin-bottom: 5px; color: #2c3e50; }
        .book-author { color: #7f8c8d; font-size: 0.85em; margin-bottom: 15px; }
        .mood-badge { display: inline-block; background: #e1ecf4; color: #39739d; padding: 5px 12px; border-radius: 20px; font-size: 0.75em; align-self: flex-start; }
    </style>
</head>
<body>

    <nav>
        <div class="nav-brand">📚 Pustaka Kreatif</div>
        <div class="nav-items">
            <% if (user) { %>
                
                <% if (user.role === 'admin') { %>
                    <a href="/admin" class="btn" style="background:#8e44ad; color:white; display:flex; gap:5px; margin-right:5px;">
                        <span>⚙️</span> Dashboard
                    </a>
                    <a href="/add-book" class="btn btn-add"><span>+</span> Tambah Buku</a>
                <% } %>

                <a href="/profile" class="profile-link">
                    <span>Hai, <b><%= user.username %></b></span>
                    <span class="level-badge">Lvl. <%= user.level %></span>
                </a>
                <a href="/logout" class="btn btn-logout">Logout</a>
            <% } else { %>
                <a href="/login" class="btn btn-login">Masuk</a>
                <a href="/register" class="btn btn-register">Daftar Akun</a>
            <% } %>
        </div>
    </nav>

    <div class="container">
        <h1>✨ Koleksi Buku Masa Depan</h1>
        
        <form action="/" method="GET" class="search-container">
            <input type="text" name="search" class="search-input" placeholder="Cari judul, penulis, atau mood..." value="<%= searchQuery %>">
            <button type="submit" class="search-btn">Cari</button>
        </form>

        <% if (books.length === 0) { %>
            <div style="text-align: center; color: #777; padding: 50px;">
                <h3>🔍 Buku tidak ditemukan</h3>
                <p>Coba kata kunci lain atau <a href="/" style="color:#3498db">Lihat Semua Buku</a></p>
            </div>
        <% } else { %>
            <div class="book-shelf">
                <% books.forEach(book => { %>
                    <a href="/book/<%= book.id %>" class="book-card">
                        <% if (book.coverImage) { %>
                            <img src="<%= book.coverImage %>" alt="Cover" class="book-cover-img">
                        <% } else { %>
                            <div class="placeholder-cover">COVER</div>
                        <% } %>
                        <div class="book-title"><%= book.title %></div>
                        <div class="book-author"><%= book.author %></div>
                        <% if(book.moodTags) { %>
                            <div style="margin-top: auto;">
                                <span class="mood-badge">✨ <%= book.moodTags %></span>
                            </div>
                        <% } %>
                    </a>
                <% }) %>
            </div>
        <% } %>
    </div>

</body>
</html>